# 浏览器插件作为工作台伴随入口

现有 Chrome 插件继续复用页面解析、定位和当前页分析能力，但从独立工具转为 localhost 运营工作台的所见即所得采集与页面核验入口。第一阶段支持从 noon 内任意兼容页面把商品作为人工补充候选同步进统一候选评估池，不受类目观察池限制；跨平台采集留给后续独立适配器。类目配置、推荐规则、候选状态、历史快照和操作日志仍由工作台统一持有，避免形成两套状态来源。
